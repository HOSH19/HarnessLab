{
  "install": "python3 -m pip install -e '.[dev]' && (grep -q '.local/bin' \"$HOME/.bashrc\" 2>/dev/null || echo 'export PATH=\"$HOME/.local/bin:$PATH\"' >> \"$HOME/.bashrc\") && mkdir -p \"$HOME/.local/bin\" && (command -v python >/dev/null 2>&1 || ln -sf \"$(command -v python3)\" \"$HOME/.local/bin/python\")"
}
